# Neurotic Nix
An absolutely minimal way to implement the Dendritic pattern or similar, without large external dependencies like flake-parts.

Import agnostic - doesn't care if you use recursve imports or manual imports. If you want a more dendritic-like experience, use recursve imports.
